// Problem 35 : Dream Car

const car = {};

car.model = "Tesla";
car.year = 2019;
car.color = "Black";
car.new = false;

console.log(
  `I'm looking for a ${car.color} ${car.year} ${car.model} that is used.`
);
